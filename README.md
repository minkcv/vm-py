## A python3 implementation of the Vapor Spec virtual architecture.

[Main Vapor Spec repo](https://github.com/minkcv/vm)

### Depends on pygame:

`python3 -m pip install pygame --user`

or try 

`sudo apt-get install python3-pygame`

### Run pong

`python3 main.py`